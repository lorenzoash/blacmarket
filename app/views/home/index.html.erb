<center>

    <div class="col-md-12 col-md-offset-3">
        <table class ="table">
            <thead>
                <tr>
                <% @items.each do |item| %>
                <%= link_to (image_tag item.image.url) , item_path(item) %>
                    
                </tr>
            </thead>
            <tbody>   
                <% end %>
            </tbody>
        </table>
    </div>
</center>